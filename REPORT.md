# 실험 보고서 — 접근 방법과 전략

Public 리더보드 최종 **0.80866 (672/831)**. 이 문서는 그 점수가 어떤 경로로 만들어졌고,
무엇이 통했고 무엇이 통하지 않았는지를 요약합니다. 일자별 원본 실험 로그는 내부 문서에
별도 보존돼 있으며, 여기서는 검증 방법론과 재현 가능한 결론만 정리합니다.

## 1. 점수 궤적 (전부 Public 실측)

| 단계 | Public | 누적 기여 |
|---|---|---|
| Base Qwen2.5-3B-Instruct greedy | ~0.70 | – |
| SFT 단독 최적 (hybrid_3145) | 0.71480 | 학습 트랙 전체 기여 +~1.5%p |
| 5-voter 앙상블 | 0.74007 | +2.5%p |
| + support4 self-consistency override | 0.74969 (623) | +1.0%p |
| + TIR(코드 실행) override 도입 | 0.75932 (631) | +1.0%p |
| + TIR self-consistency / 게이트 확장 | 0.77858 (647) | +1.9%p |
| + 코드 리페어·풀 병합 (16→24샘플) | 0.78941 (656) | +1.1%p |
| + 표수≤3 대역 3풀 24샘플 규칙 (660) | 0.79422 | +0.5%p |
| + 삼중 게이트 (GRPO 확신 × 코드가드) | 0.80024 (665) | +0.6%p |
| + few-shot 포인터 게이트 | **0.80866 (672)** | +0.8%p |

핵심 요약: **누적 개선의 대부분이 학습이 아니라 추론 하네스(앙상블 규칙·self-consistency·
코드 실행·게이트)에서 나왔습니다.** 3B 단일 베이스 제약에서는 test-time compute 의
설계가 파인튜닝보다 레버리지가 컸습니다.

## 2. 검증 방법론

- **누수 방지 홀드아웃**: 공식 train 에서 학습에 쓰지 않은 500문제를 고정하고, 전수
  재검증으로 라벨 오류·손상 36문제(7.2%)를 제외한 **clean holdout464** 를 주 판정 기준으로
  사용. 보조로 fixed200(스모크), pool600(규칙 개발)을 분리 운용.
- **판정 규칙(자체 강제)**: ① 비교 기준은 항상 "현행 최고 제출본"의 홀드아웃 등가물
  (baseline 대비 측정은 이득을 과대평가함을 실측) ② 464문제에서 ±4는 노이즈로 간주
  ③ 변경 문항 15개 미만 후보는 홀드아웃 성적과 무관하게 미검증 취급 ④ 홀드아웃 먼저,
  리더보드는 마지막. 실제로 이 규칙을 어긴 제출은 대부분 하락했고(-6, -1, -1),
  지킨 제출이 +4, +5 를 냈습니다.
- **과적합 관리**: 최종 평가가 별도 세트임을 전제로, 리더보드 831에 대한 probing 성
  제출을 금지하고 홀드아웃 통과 후보만 제출. 실측된 홀드아웃→Public 부풀림은 평균
  +5.25/제출로, 최종 세트에서도 유사한 일반화를 기대합니다.

## 3. 통한 것

1. **Self-consistency 의 타겟 적용.** 전면 다수결(N=8)은 5-voter 보다 오히려 나빴고,
   5-voter 가 정확히 4/5로 합의한 구간(support=4)에만 적용했을 때 순이득이
   fixed200·홀드아웃·Public 세 곳에서 재현됐습니다.
2. **TIR (Tool-Integrated Reasoning).** 모델이 파이썬 코드를 생성→로컬 실행→정수 출력
   검증. 코드 실행 정답률(22.6%)이 미실행(5.4%)의 4배였고, 정수론·조합론 오답의 대부분을
   이 경로가 회수했습니다. 이후 실패 코드 자동 리페어(1라운드), 코드 미생성 재시도,
   독립 시드 풀 병합(24샘플이 최적점)으로 확장.
3. **GRPO (RLVR).** 공식 train 에서 pass-rate 2~6/8 대역 667문제 선별 → hybrid_3145 에서
   이어 학습. 단독 greedy 를 357→365/464 로 올린 유일한 학습 방법. 단, 앙상블 교체로는
   기여가 없었고(아래 4절), **확신 신호(N=8 유일최빈 ≥5표)로 게이트에 끼웠을 때만**
   Public +5 로 전환됐습니다.
4. **독립 신호의 교집합.** 단일 신호(홀드아웃 델타)는 Public 에서 배신하는 일이 잦았지만,
   "팀 약함(support≤4) × GRPO 확신 × 코드 비반대"의 교집합은 그대로 전이됐습니다.
   같은 신호원의 2차 수확(문턱 완화·확장판)은 전부 음수 — 신호 우물은 첫 수확만 유효.
5. **few-shot 포인터 게이트 (최종 +7).** few-shot(3-shot) 프롬프트는 단독으로는
   해로웠지만(-13), 앙상블이 못 열던 문제를 가리키는 분포-시프트 포인터로는
   유효했습니다. 포인터(few-shot greedy가 현행 답과 다름)와 확인(독립 2계보 16샘플
   풀의 상대다수 + few-shot stochastic N=8 자기재현 ≥2표)을 분리한 사전 등록 규칙로
   홀드아웃 gain 9/reg 0 → Public +7 이 그대로 전이됐습니다.

## 4. 통하지 않은 것 (전부 실측 기각)

- **SFT 확장 6종**: 데이터 3배(external_10000), 고난도 추가, verbose CoT 증류,
  R1 long-CoT 증류, 32B teacher 증류, TIR 자기증류 — 흡수가 거의 0이거나 역효과.
  병목은 teacher 품질이 아니라 규모(3B LoRA 소량 SFT 로는 스타일·능력이 안 옮음).
- **학습형 selector/verifier 4종(누적 11회)**: 이진 분류·pairwise·outcome 라벨링·
  역검증 — 전부 dev 단계 미달. 반면 min-count 류 단순 통계 규칙은 4개 셋에서 재현.
- **N 증가 단독**(8→16→64): oracle 만 오르고 실채택 전환 0.
- **GRPO 의 앙상블 통합 5경로**(투표자 교체/추가, SC 샘플러 교체, TIR 재료 교체,
  동점 심판): 전부 0 이하. 단독 +8 모델도 기존 앙상블과 정답 영역이 겹치면 기여가 없음
  ("다양성 없는 실력 향상"). 게이트 방식(3절 4항)만 예외.
- **SymPy 기호 TIR**: 실행은 되나(성공 68%) 대수 잔여 오답의 병목은 계산이 아니라
  식 세우기 추론이라 순이득 없음.

## 5. 남은 오답의 구조 (clean holdout464 기준, 최종 672 등가 시점 68개)

- 오라클 등급: 완전 미해결 15개(22%) / 생성 산출물 어딘가에 정답 존재 53개(78%) —
  정답이 있어도 채택 규칙이 못 고르는 **선택 병목**이 마지막까지 최대 병목.
- 분야: 대수 48% (TIR 도입 전 지배적이던 정수론+조합론 54% → 15%로 감소.
  숫자 계산 문제는 코드 실행이 사실상 정복).

## 6. 사후 검증 — 신선한 2,000문제 홀드아웃

체인이 공개 평가 구간에 과적합됐는지 확인하기 위해, 어떤 튜닝에도 쓰지 않은 공식
train 잔여 2,000문제에 전체 파이프라인을 처음부터 재실행해 채점했습니다
(2026-08-31, 최종 제출 직전):

| 체인 단계 | 정확도 | 직전 대비 |
|---|---|---|
| base greedy | 64.80% | – |
| 다수결+SC (623 등가) | 67.80% | +60문제 (g80/r20) |
| +TIR (660 등가) | 71.30% | +70 (g88/r18) |
| +삼중 게이트 (665 등가) | 72.30% | +20 (g25/r5) |
| +few-shot 게이트 (672 등가, 최종) | **72.65%** | +7 (g18/r11) |

**모든 층이 신선한 셋에서도 순양(+) 전이했고 후보 간 순위가 Public 과 일치** —
체인이 평가 구간의 우연이 아니라 규칙으로 작동함을 확인했습니다. (절대치가 Public
보다 낮은 것은 이 2,000문제가 더 어려운 분포이기 때문입니다.)

## 7. 재현성

- 제출본 조립 체인 전 단계가 `scripts/reproduce_all.sh compose` 로 보관 산출물에서
  **바이트 단위(sha256) 재조립**됨을 확인했습니다 (이 저장소 상태에서 재실행 가능).
- 확률적 생성 단계까지 포함한 전체 경로는 `full` 모드로 재현합니다 (GPU·엔진 차이에
  따른 부동소수점 순서 변화로 바이트 일치는 원리적으로 불가, 규칙·절차 재현 확인용).
- 학습 로그: GRPO 전 구간 `logs/grpo_scaleup_train.log`, loss 곡선 데이터는 ck150 의
  `trainer_state.json`. SFT 는 초기 pod 환경과 함께 로그가 소실되어 하이퍼파라미터
  스냅샷(`training_metadata.json`, 데이터 sha256 포함)으로 대체합니다. wandb 미사용.
